define(['react', 'app/Rest', 'app/Store'], 
function(React, Rest, Store, Banner, BlockQuotes, Sitemap) {
  const { PropTypes, Component } = React

  class Home extends Component {
    componentDidMount() {

    }

    render() {
      return (
        <div className='auth wrap'>

          
        </div>

      );
    }
  }

  return Home
})
