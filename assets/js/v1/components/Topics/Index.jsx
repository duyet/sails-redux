define(['react'], function(React) {
  const { PropTypes } = React

  const Content = ({ active, children }) => {
    return (
      <h1>Topics</h1>
    )
  }

  Content.propTypes = {
  }

  return Content
})
